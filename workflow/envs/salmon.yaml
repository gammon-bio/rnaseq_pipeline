# Seeded from environment.yml (salmon=1.10.3); pinned to major.minor.
# Matches the salmon version that built the prebuilt mouse index (index_version 4, sshash).
channels:
  - conda-forge
  - bioconda
dependencies:
  - salmon=1.10
